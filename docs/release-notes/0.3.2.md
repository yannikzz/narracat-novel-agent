这一版可以用 **Kimi** 写小说了。

- 「设置 → 模型服务」里新增 Kimi 渠道，填上月之暗面的 API Key 就能用，内置 `kimi-k3`（100 万字上下文）与 `kimi-k2.6` 两款
- 和其他渠道一样支持设为主力或轻量模型，可以只在部分环节用它

顺手改了配置模型服务时的两处别扭：

- 「测试连接」挪到了配置流程的最后一步——它本来排在填 Key 和填接口地址中间，让人在还没选模型时就去点它
- 拉不到模型清单的渠道不再显示「刷新清单」按钮，省得点了才知道不支持
